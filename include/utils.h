#ifndef UTILS_HEADER
#define UTILS_HEADER

#include "types.h"

real_t GetEuclideanDistance( const Vector2*, const Vector2*);
real_t GetSquaredSum( const Vector2*, const Vector2*);
real_t GetTriangleArea(const Vector2*, const Vector2*, const Vector2*);

#endif
